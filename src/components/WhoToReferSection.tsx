import { Button } from "./ui/button";
import { motion } from "motion/react";

export function WhoToReferSection() {
  const scrollToSection = (id: string) => {
    document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' });
  };

  return (
    <></>
    
  );
}